name: 003-002-003 Deploy EKS Auto IRSA
on:
  workflow_dispatch:

jobs:
  tflint-account-resources:
    name: Run TFLint - (Project Account Resources)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6